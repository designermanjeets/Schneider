"use strict";

angular.module('conext_gateway.setup').controller('RebootInProgressController',
  ['$scope',
  function($scope) {
    //$scope.$on('modal.closing', function(event) {
    //  event.preventDefault();
    //});
  }
]);
